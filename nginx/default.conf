daemon off;

server {
    listen       3000;
    server_name  _;
    
    root /app/static;

    location /api {
        proxy_pass       http://127.0.0.1:8080;
    }
    
    location / {
    	index index.html;
    }
    
    location /static/ {
    }
    
    client_max_body_size 100M;
}
